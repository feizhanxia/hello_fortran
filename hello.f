        PROGRAM hello_fortran
        INTEGER :: a, b, c
        WRITE (*,*) 'Input a & b to multiply'
        READ (*,*) i, j
        k=i*j
        WRITE (*,*) 'Result: a * b = ', k
        STOP
        END PROGRAM hello_fortran
